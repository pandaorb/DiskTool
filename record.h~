#ifndef RECORD_H
#define RECORD_H

#include<iostream>
using namespace std;

class Record
{
	string attribute;
	unsigned long value;
	public:
	Record();
	Record(string newAttribute, unsigned long newValue);
	string getAttribute();
	void setAttribute();
	unsigned long getValue();
	void setValue();
};

#endif
